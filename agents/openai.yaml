interface:
  display_name: "Autoresearch"
  short_description: "Reach measurable goals through verified experiments"
  brand_color: "#2563EB"
  default_prompt: "Use $autoresearch to reach a numeric repository goal through verified experiments."

policy:
  allow_implicit_invocation: false
